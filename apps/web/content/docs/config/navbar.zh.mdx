---
title: 导航栏配置
description: 配置网站头部的导航菜单
icon: Menu
---

定义网站头部的导航菜单链接，每个项目都是 [NestedMenuItem](/docs/config#nestedmenuitem)。

```tsx title="src/config/navbar-config.tsx"
export function getNavbarLinks(): NestedMenuItem[] {
  const t = useTranslations('Marketing.navbar');

  return [
    {
      title: t('features.title'),
      href: Routes.Features,
      external: false,
    },
    {
      title: t('pricing.title'),
      href: Routes.Pricing,
      external: false,
    },
    {
      title: t('blocks.title'),
      items: [
        {
          title: t('blocks.items.hero-section.title'),
          icon: <FlameIcon className="size-4 shrink-0" />,
          href: Routes.HeroBlocks,
          external: false,
        },
        // 更多下拉项...
      ],
    },
    // 更多导航项...
  ];
}
```

导航栏支持简单链接和带有嵌套项的下拉菜单。下拉菜单可以包含图标和描述，以提供更丰富的用户体验。

## 常见问题

### 如何改变导航栏的菜单布局？

导航栏的菜单布局定义在 `src/components/shared/navbar.tsx` 文件中，使用的是 Shadcn UI 中的 [NavigationMenu](https://ui.shadcn.com/docs/components/navigation-menu) 组件。如图所示，如果您想将菜单布局更改为垂直菜单，可以参考 Shadcn UI 文档中的代码。

## 下一步

现在您了解了导航栏配置，请探索其他相关主题：

<Cards>
  <Card icon={<Layout className="text-blue-500" />} title="网站配置" href="/docs/config/website">
    配置网站的核心设置
  </Card>

  <Card icon={<Footprints className="text-green-500" />} title="页脚配置" href="/docs/config/footer">
    配置网站页脚中的链接
  </Card>

  <Card icon={<SidebarOpen className="text-amber-500" />} title="侧边栏配置" href="/docs/config/sidebar">
    配置仪表盘的导航菜单
  </Card>

  <Card icon={<CircleUser className="text-purple-500" />} title="头像配置" href="/docs/config/avatar">
    配置用户头像下拉菜单
  </Card>
</Cards>
